name: gpu-lm-training
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python>=3.9.*
  - pip=23.*
  - black
  - mypy
  - pytorch=2.*
  - pytorch-cuda=11.*
  - transformers=4.*
  - accelerate
  - sentencepiece=0.*
  - hydra-core=1.3.*
  # - librosa=0.* # add if audio processing is needed
  # - pysoundfile>=0.12.1 # add if audio processing is needed
